const camera = () => {
    
    // Return the 9 colors of one side only of the cube.
    // This function will be called 6 times (6 sides of the cube).

    return (["red", "blue", "green", "white", "red", "green", "orange", "blue", "white"]);
}

export default camera;